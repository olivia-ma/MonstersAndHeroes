package pieces.creatures;

// type safe enumerations enabling polymorphic behavior without subclass explosion
// supports flexible hero variety while maintaining single class hierachies 

public enum HeroType { WARRIOR, SORCERER, PALADIN }
